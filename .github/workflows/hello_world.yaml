# name
name: hello_world

# trigger
on:
  - push

# one or more jobs
jobs: 
  greetings:
    runs-on: ubuntu-latest
    steps:
    - id: s00
      run: echo "Current time is $(date)"
      #running some shell commands in bash
    - id: s01
      run: ls